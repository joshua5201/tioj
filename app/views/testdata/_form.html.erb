<%= form_for [@problem, @testdatum] do |f| %>
    <% if @testdatum.errors.any? %>
        <div id="error_explanation">
        <h2><%= pluralize(@testdatum.errors.count, "error") %> prohibited this testdatum from being saved:</h2>

        <ul>
            <% @testdatum.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
           <% end %>
     </ul>
</div>
    <% end %>

<div class="field">
    <%= f.label :input %><br>
    <%= f.text_area :input %>
</div>
<div class="field">
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
</div>
<div class="actions">
    <%= f.submit %>
</div>
<% end %>
